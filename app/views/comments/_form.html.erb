<%= form_for([@product, @product.comments.build]) do |f| %>
  <p>
    <%= f.label :komentujacy %><br>
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :komentarz %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>